def sum_positive_numbers(numbers: list) -> int:
    # Goal: Sum only numbers > 0.

    total = 0
    # Starting point for our sum.

for num in numbers:
    # Iterate through the list.

    if num > 0:
        total += num
        # Add the current number to our running tota.

return total

        