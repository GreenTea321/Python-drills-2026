def format_team(players_list):
    team_string = " ".join(player_list)
    # Syntax: " ".join() is a string method that takes a list.
    # Logic: GLues strings together with a space in between them.
    # Performance: O(n); much faster than using '+' in a loop.

    return team_string
    # Logic: Returns a single string (e.g., "Kyo Iori Chizuru").

